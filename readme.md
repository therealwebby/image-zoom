Image zoom application with React and Redux

## Getting Started

- Install dependencies `npm install`  
- Run the dev server `npm run dev`  

## Other tasks
- `npm build` - builds out packages for production
- `npm test` - runs Jest unit tests
